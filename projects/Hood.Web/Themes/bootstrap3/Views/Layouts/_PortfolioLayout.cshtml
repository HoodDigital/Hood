@{ 
    Layout = "_Layout";
}
@section styles {
    @RenderSection("styles", required: false)
}
@section metas {
    @RenderSection("metas", required: false)
}
<div class="jumbotron">
    <div class="container">
        <h1>Portfolio Layout</h1>
        <p>This is the Portfolio Layout - it can be overwritten in your theme directory by copying <code>/Views/Layouts/_PortfolioLayout.cshtml</code> to match in your themes directory <code>/Themes/{YOURTHEME}/Views/Layouts/_PortfolioLayout.cshtml</code></p>
    </div>
</div>
<section class="container">
    @RenderBody()
</section>
