@model ForumModel
@if (string.IsNullOrEmpty(Model.Search))
{
    <alert type="Info">
        There are no forums available right now, maybe check back later?
    </alert>
}
else
{
    <alert type="Info">
        Your search '@Model.Search' did not return any results. Please try searching for something else.
    </alert>
}
