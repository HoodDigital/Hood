@{ 
    Layout = "_Layout";
}
@section styles {
    @RenderSection("styles", required: false)
}
@section metas {
    @RenderSection("metas", required: false)
}
<div class="jumbotron">
    <div class="container">
        <h1>Product Layout</h1>
        <p>This is the Product Layout - it can be overwritten in your theme directory by copying <code>/Views/Layouts/_ProductLayout.cshtml</code> to match in your themes directory <code>/Themes/{YOURTHEME}/Views/Layouts/_ProductLayout.cshtml</code></p>
    </div>
</div>
<section class="container">
    @RenderBody()
</section>
